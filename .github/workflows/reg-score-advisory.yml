name: reg-score advisory

on:
  pull_request:
    paths-ignore:
      - 'docs/**'
      - '*.md'

permissions:
  contents: read
  pull-requests: write

jobs:
  advisory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - run: npm ci
      - run: npm run build

      - name: reg-score diff advisory
        run: |
          npm run reg-score -- diff . --base origin/${{ github.base_ref }} --format markdown \
            --github-summary "$RUNNER_TEMP/reg-score-summary.md" \
            --github-annotations "$RUNNER_TEMP/reg-score-annotations.txt"

      - name: policy evaluate (advisory)
        run: npm run reg-score -- policy . --evaluate
        continue-on-error: true

      - name: Publish job summary
        if: always() && hashFiles(format('{0}/reg-score-summary.md', runner.temp)) != ''
        run: cat "$RUNNER_TEMP/reg-score-summary.md" >> "$GITHUB_STEP_SUMMARY"

      - name: Publish PR annotations
        if: always() && hashFiles(format('{0}/reg-score-annotations.txt', runner.temp)) != ''
        run: cat "$RUNNER_TEMP/reg-score-annotations.txt"
