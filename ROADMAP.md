# reg-score プロダクトロードマップ

## 1. ビジョン

AI コーディングエージェントによる変更を繰り返すうちに、実装者が気付かないままコードベースがデグレしやすい構造へ変化し、修正のたびに別の不具合を生む状態がある。

reg-score は、その状態を不具合の発生前に検出する。コードの構造、依存関係、テスト、変更履歴を決定論的に観測し、LLM で責務や暗黙契約を意味的に分析することで、次を説明可能な診断として提供する。

1. 現在どの程度デグレが発生しやすいか。
2. どの構造とファイル群が危険か。
3. どのような変更を契機に、なぜデグレが発生するか。
4. 開発停止へ至る前に、何をどの順番で改善すべきか。

## 2. プロダクト原則

- **予防を目的にする** — 現在のバグではなく、将来の変更に対する脆弱性を扱う。
- **スコアより根拠を優先する** — 数字だけでなく、ファイル、依存関係、観測事実、因果経路を示す。
- **リスクと確信度を分ける** — 高リスクだが証拠が弱い診断を、確定的に表現しない。
- **LLM 単独で採点しない** — 決定論的なリスクシグナルに LLM の意味解析を重ねる。
- **打ち手を検証可能にする** — 提案ごとに軽減対象と再診断方法を示す。
- **時系列で判断する** — 絶対値に加え、ベースラインからのリスク差分を重視する。
- **段階的に言語を増やす** — プロダクト非依存を目指しつつ、最初から全言語を浅く扱わない。
- **利用者のコードを守る** — LLM へ渡す範囲、除外、秘匿化、プロバイダーを明示する。

## 3. プロダクト境界

### 対象

- リポジトリ全体の構造的なデグレリスク診断
- ファイル単体ではなく、関係するファイル群のリスククラスター分析
- PR またはコミット差分によるリスク増減の診断
- リスク原因、想定されるトリガー変更、発生メカニズムの説明
- 優先順位と確認方法を伴う改善提案

### 対象外

- 現在存在するすべての不具合の検出
- 将来の障害発生確率の保証
- LLM の自由裁量による自動リファクタリング
- テスト、型検査、静的解析、コードレビューの代替
- 根拠のない「コード品質」の総合評価

## 4. 診断モデル

### 4.1 出力単位

診断結果は次の階層で表現する。

1. **Repository Assessment** — リポジトリ全体の Regression Risk Score、確信度、前回差分
2. **Risk Axis Assessment** — 観点別スコアと寄与度
3. **Risk Cluster** — 関連する構造・ファイル群と発生メカニズム
4. **Evidence** — コード、依存辺、テスト欠落、履歴、意味所見などの根拠
5. **Intervention** — 優先順位付きの打ち手と、改善を確認する再診断条件

### 4.2 初期評価軸

| 評価軸 | 評価する性質 | 主なリスクシグナル |
|---|---|---|
| Structural Fragility | 責務集中、密結合、循環、境界違反 | 依存グラフ、複雑性、責務の混在 |
| Change Blast Radius | 小さな変更が広範囲へ波及する度合い | 推移依存、共有契約、fan-in / fan-out |
| Verification Gap | 変更影響に対して検証が不足する度合い | テスト対応、境界テスト、変更経路との不一致 |
| Change Volatility | 頻繁な変更が不安定な領域へ集中する度合い | churn、revert、修正反復、共変更履歴 |
| Semantic Ambiguity | 意図や契約を復元しにくい度合い | 暗黙ルール、重複ルール、命名乖離、例外的分岐 |

### 4.3 スコア契約

- Regression Risk Score は 0 から 100 とし、高いほどデグレリスクが高い。
- スコアは異なるリポジトリ間の絶対的な優劣ではなく、同一評価契約内の優先順位と時系列比較に使う。
- リポジトリ全体の平均で重大な局所リスクを隠さない。重大なリスククラスターと波及範囲を反映する集約方法を定義する。
- すべてのスコアに評価契約バージョン、入力識別子、利用した解析器、確信度を付ける。
- LLM の出力は直接点数にせず、schema 検証済みの意味所見として Risk Assessment へ渡す。

## 5. 利用体験の到達像

```bash
reg-score scan . --format markdown
reg-score diff . --base origin/main --format json
```

利用者は一度の診断で次を得る。

- Regression Risk Score と前回からの増減
- 観点別スコアと、総合リスクへの寄与
- 優先度順のリスククラスター
- 各クラスターに属するファイルと依存経路
- 「どの変更が、どの弱点を通り、何を壊し得るか」という因果説明
- すぐ行う防御策と、中期的な構造改善策
- 診断の確信度、前提、解析できなかった領域

## 6. フェーズ

### Phase 0 — 評価契約と検証コーパス

**目的:** 実装前に「何を測り、数字をどう解釈するか」を固定する。

**成果物:**

- versioned report schema
- 評価軸、スコア方向、確信度、集約規則の仕様
- 安定した構造と脆弱な構造を対にした小規模 fixture repository 群
- 人間が期待するリスククラスター、原因、打ち手を記録した golden assessments
- LLM 不使用でも再現できる決定論的シグナルの一覧

**完了条件:**

- 同一入力と同一設定で決定論的シグナルが再現する。
- fixture ごとに、危険箇所と想定される発生メカニズムを人間がレビューできる。
- スコアが障害発生確率ではないことを schema と表示で誤解なく表せる。

### Phase 1 — TypeScript / JavaScript 向けリポジトリ診断 MVP

**目的:** ローカルリポジトリを一回走査し、説明可能なリスクマップを生成する。

**成果物:**

- Node.js 22 / TypeScript 製 CLI
- import、依存関係、複雑性、Git 履歴、テスト配置の抽出
- LLM provider adapter と構造化された意味所見
- Repository / Axis / Cluster / Evidence / Intervention の診断生成
- console、Markdown、JSON 出力
- 除外パス、解析上限、コード送信範囲の設定

**完了条件:**

- `reg-score scan <path>` が 1 つの TypeScript / JavaScript リポジトリを変更せず診断できる。
- 上位リスククラスターごとに複数ファイル、原因、トリガー変更、打ち手を示せる。
- LLM が利用不能でも、決定論的診断と未評価項目を出力できる。

### Phase 2 — 打ち手の有効性と再診断

**目的:** 一般論ではなく、観測された原因を減らす具体策を提示する。

**成果物:**

- 発生メカニズムと打ち手を対応付ける recommendation rules
- 「変更前に追加するテスト」と「構造を改善する変更」の分離
- 改善候補ごとの期待効果、コスト、確認方法
- before / after 診断比較

**完了条件:**

- 各打ち手が少なくとも 1 つのリスクシグナルへ追跡可能である。
- fixture の改善版で対象リスクが下がり、無関係な評価軸を恣意的に変えない。

### Phase 3 — 差分診断と PR アドバイザー

**目的:** AI による変更が脆弱性を増やした時点で、マージ前に知らせる。

**成果物:**

- `reg-score diff --base <ref>`
- ベースライン保存と評価契約の互換性検証
- 変更ファイルからの blast radius とリスク差分
- GitHub Actions の Job Summary と PR annotation
- advisory mode の閾値設定

**完了条件:**

- スコア増減だけでなく、新規・悪化・改善したリスクシグナルを説明できる。
- 評価契約が異なる結果を誤って比較しない。
- 初期状態では CI を自動的に失敗させず、判断材料として提示する。

### Phase 4 — 校正と信頼性

**目的:** スコアを印象値から、検証可能な意思決定支援へ進化させる。

**成果物:**

- 実際の regression / revert / hotfix と診断結果を結ぶ評価データ
- 誤検知、見逃し、順位品質、説明有用性の測定
- リポジトリ特性に応じた基準線の校正
- モデル変更時の golden assessment 回帰テスト
- 診断フィードバックの収集形式

**完了条件:**

- スコア帯ごとの観測結果とサンプル数を公開できる。
- LLM または重みの変更で診断品質が悪化した場合に検出できる。
- CI gate に使える条件と、使えない条件を明示できる。

### Phase 5 — 言語・解析器プラグイン

**目的:** 評価モデルを保ったまま、さまざまなプロダクトへ適用する。

**成果物:**

- versioned analyzer plugin contract
- Python、Go など次言語の evidence extractor
- monorepo の package / service 単位診断
- LLM provider、Git provider、reporter の adapter 拡張
- capability negotiation と未対応シグナルの明示

**完了条件:**

- 言語固有解析器が共通の Evidence Set を生成する。
- 利用できない解析能力をゼロ点として扱わず、確信度と未評価項目へ反映する。
- 複数言語リポジトリでも評価根拠をファイルまで追跡できる。

### Phase 6 — 継続診断とチーム運用

**目的:** リスクの蓄積を継続的に監視し、開発停止へ至る前に介入する。

**成果物:**

- リポジトリとリスククラスターの時系列トレンド
- チーム固有の警告・承認ポリシー
- 改善投資の優先順位ビュー
- 校正済み条件に限定した opt-in CI gate
- 診断結果の保持、秘匿化、監査ポリシー

**完了条件:**

- スコア悪化の開始点と寄与した変更群を追跡できる。
- 高リスク状態での推奨対応がチームの開発フローに組み込まれる。
- gate の判断根拠を人間が再現・監査できる。

### フェーズ完了状況（2026-09-05）

| Phase | 状態 | 検証 |
|---|---|---|
| 0 | 完了 | `docs/spec/*`, `src/schema/report.v1.ts`, golden fixtures |
| 1 | 完了 | `reg-score scan`, vitest 36件 |
| 2 | 完了 | `fragile-cart-improved` fixture, `tests/intervention.test.ts` |
| 3 | 完了 | `reg-score diff`, blast radius, GitHub advisory workflow |
| 4 | 完了 | calibration メトリクス, `calibration --golden`, feedback 形式 |
| 5 | 完了 | provider adapters, `--unit`, Python/Go スタブ |
| 6 | 完了 | `trend --analyze` + commit 追跡, `priorities`, audit ポリシー |

## 7. 最初のリリース境界

最初の公開可能なリリースは Phase 0 と Phase 1 に限定する。

- 対象はローカルの TypeScript / JavaScript リポジトリ。
- snapshot 診断のみを提供し、PR gate とダッシュボードは含めない。
- Markdown と JSON の read-only レポートを生成する。
- 総合スコア、評価軸、上位リスククラスター、根拠、打ち手、確信度を必須出力にする。
- npm 公開前は `private: true` を維持し、名称・ライセンス・送信データ方針を確認する。

## 8. 主要リスク

| リスク | 影響 | 抑制策 |
|---|---|---|
| LLM の印象値がスコアになる | 再現性と信頼を失う | 決定論的シグナル、構造化所見、根拠参照、確信度を分離する |
| リポジトリ間で数字を誤比較する | 間違った優先順位を生む | 同一契約内の時系列比較を主用途にする |
| 大規模リポジトリで文脈を失う | 重要な依存や暗黙契約を見逃す | グラフでクラスター化してから限定文脈を LLM へ渡す |
| 一般論の改善提案になる | 実行されず価値が出ない | 各打ち手を原因、対象ファイル、確認方法へ結び付ける |
| コード送信が情報漏えいになる | 導入できない | local-first 収集、明示的送信範囲、除外、adapter 化を行う |
| スコア攻略が目的になる | 実品質と指標が乖離する | 単一閾値より根拠と時系列を重視し、評価契約変更を版管理する |

## 9. 判断保留事項

次の項目は、Phase 0 の証拠を得てから ADR として決定する。

- 初期 LLM プロバイダーとローカルモデル対応範囲
- AST / language server / 外部解析器の選択
- スコア集約式と評価軸ごとの重み
- 診断キャッシュとベースラインの保存形式
- npm の最終 package scope と公開ライセンス
- リスクスコアを CI gate に利用できる校正条件
